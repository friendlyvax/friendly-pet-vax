import { MapContainer, TileLayer, Marker, Popup } from 'react-leaflet';
import 'leaflet/dist/leaflet.css';
import L from 'leaflet';

// Optional: set a custom marker icon if needed
delete L.Icon.Default.prototype._getIconUrl;
L.Icon.Default.mergeOptions({
  iconRetinaUrl: require('leaflet/dist/images/marker-icon-2x.png'),
  iconUrl: require('leaflet/dist/images/marker-icon.png'),
  shadowUrl: require('leaflet/dist/images/marker-shadow.png'),
});

export default function Location() {
  return (
    <div>
      <h2 className="text-2xl font-bold mb-4 text-green-700">Find Us</h2>
      <MapContainer center={[37.7749, -122.4194]} zoom={13} className="h-96 w-full rounded shadow">
        <TileLayer
          attribution='&copy; OpenStreetMap contributors'
          url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"
        />
        <Marker position={[37.7749, -122.4194]}>
          <Popup>
            FriendlyVax HQ<br />123 Pet Lane, San Francisco, CA
          </Popup>
        </Marker>
      </MapContainer>
    </div>   
  );
}
import { MapContainer, TileLayer, Marker, Popup } from 'react-leaflet';
import 'leaflet/dist/leaflet.css';
import L from 'leaflet';

// Optional: set a custom marker icon if needed
delete L.Icon.Default.prototype._getIconUrl;
L.Icon.Default.mergeOptions({
  iconRetinaUrl: require('leaflet/dist/images/marker-icon-2x.png'),
  iconUrl: require('leaflet/dist/images/marker-icon.png'),
  shadowUrl: require('leaflet/dist/images/marker-shadow.png'),
});

export default function Location() {
  return (
    <div>
      <h2 className="text-2xl font-bold mb-4 text-green-700">Find Us</h2>
      <MapContainer center={[37.7749, -122.4194]} zoom={13} className="h-96 w-full rounded shadow">
        <TileLayer
          attribution='&copy; OpenStreetMap contributors'
          url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"
        />
        <Marker position={[37.7749, -122.4194]}>
          <Popup>
            FriendlyVax HQ<br />123 Pet Lane, San Francisco, CA
          </Popup>
        </Marker>
      </MapContainer>
    </div>
  );
}

